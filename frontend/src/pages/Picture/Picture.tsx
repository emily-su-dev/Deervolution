import React from "react";

const Picture: React.FC = () => {
    return (
        <div>
            <h1>📷 Capture Your Animal Sighting</h1>
            <p>Upload a picture of the animal you found!</p>
            {/* Add a file upload or camera capture input here later */}
        </div>
    );
};

export default Picture;
